const reports = document.querySelector(".container .marketing");
const btn = document.getElementById('analytics');

console.log('test');

btn.addEventListener('click', evt => {
    window.scrollBy(0, 400);
});